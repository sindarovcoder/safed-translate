const profileHandler = require("./profile");
const addressHandler = require("./address");
const orderHandler = require("./order");
const authHandler = require("./auth");
const pvzHandler = require("./pvz");

module.exports = {
  profileHandler,
  addressHandler,
  orderHandler,
  authHandler,
  pvzHandler,
};
